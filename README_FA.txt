Signal v1.7-test

تغییرات این نسخه:

1) ساعت معتبر معاملات
- اسکن زنده، سیگنال و آمار اصلی فقط بین 09:00 تا 12:30 انجام می‌شود.
- پیش‌گشایش 08:45 تا 09:00 حذف است.
- داده‌های بعد از 12:30 نیز وارد تحلیل اصلی نمی‌شوند.
- همین فیلتر روی تحلیل تاریخی صف و بررسی روز معاملاتی بعد اعمال شده است.

2) Universe اصلی
- سهام بورس
- سهام فرابورس
- سهام بازار پایه
- صندوق‌های اهرمی
ابزارهای ناسازگار با مدل فعلی از استخراج/تحلیل/سیگنال حذف مانده‌اند.

3) امنیت پایه Release
- cleartext HTTP غیرفعال؛ فقط HTTPS مجاز است.
- Backup و Device Transfer برای داده‌های برنامه غیرفعال/مسدود شده است.
- Release با R8 و resource shrinking ساخته می‌شود.
- Workflow فقط APK Release با Signing ثابت می‌سازد و در نبود Secrets متوقف می‌شود.
- هیچ keystore یا رمز Signing داخل سورس قرار نگرفته است.

4) رابط کاربری
- رابط جدید یکدست Signal حفظ شده است.
- صفحه سیگنال، استخراج داده، بک‌تست، جستجو و Paper Trading با طراحی جدید باقی مانده‌اند.

versionCode=21
versionName=1.7-test
